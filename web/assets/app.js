const SIZE=15,CELL=40,MARGIN=30,RADIUS=13,POLL_MS=1000;
const state={sessionId:localStorage.getItem("wuziqi_session")||"",screen:"landing",mode:"local",local:null,room:null,roomCode:"",hoverPoint:null,leavingRoom:false,mediaRecorder:null,voiceChunks:[],voiceStartAt:0,voiceTimer:null,roomSocket:null,roomSocketReconnect:null,suppressSocketReconnect:false};
const els={landingScreen:document.getElementById("landingScreen"),onlineSetupScreen:document.getElementById("onlineSetupScreen"),gameScreen:document.getElementById("gameScreen"),goLocalBtn:document.getElementById("goLocalBtn"),goOnlineBtn:document.getElementById("goOnlineBtn"),backToHomeFromSetupBtn:document.getElementById("backToHomeFromSetupBtn"),backHomeBtn:document.getElementById("backHomeBtn"),setupStatus:document.getElementById("setupStatus"),board:document.getElementById("board"),status:document.getElementById("status"),modeLabel:document.getElementById("modeLabel"),turnLabel:document.getElementById("turnLabel"),turnTimerLabel:document.getElementById("turnTimerLabel"),hintUsageLabel:document.getElementById("hintUsageLabel"),lastMoveLabel:document.getElementById("lastMoveLabel"),linkStateLabel:document.getElementById("linkStateLabel"),depthSelect:document.getElementById("depthSelect"),startLocalBtn:document.getElementById("startLocalBtn"),undoLocalBtn:document.getElementById("undoLocalBtn"),playerNameInput:document.getElementById("playerNameInput"),turnLimitSelect:document.getElementById("turnLimitSelect"),createRoomBtn:document.getElementById("createRoomBtn"),roomCodeInput:document.getElementById("roomCodeInput"),joinRoomBtn:document.getElementById("joinRoomBtn"),roomStatusText:document.getElementById("roomStatusText"),roomCodePill:document.getElementById("roomCodePill"),copyCreatedRoomBtn:document.getElementById("copyCreatedRoomBtn"),enterCreatedRoomBtn:document.getElementById("enterCreatedRoomBtn"),leaveSetupRoomBtn:document.getElementById("leaveSetupRoomBtn"),roomPlayerList:document.getElementById("roomPlayerList"),hintRoomBtn:document.getElementById("hintRoomBtn"),undoRoomBtn:document.getElementById("undoRoomBtn"),resetRoomBtn:document.getElementById("resetRoomBtn"),acceptUndoBtn:document.getElementById("acceptUndoBtn"),rejectUndoBtn:document.getElementById("rejectUndoBtn"),hintPanel:document.getElementById("hintPanel"),hintMoveLabel:document.getElementById("hintMoveLabel"),hintReasonLabel:document.getElementById("hintReasonLabel"),localActions:document.getElementById("localActions"),onlineActions:document.getElementById("onlineActions"),chatCard:document.getElementById("chatCard"),chatMessages:document.getElementById("chatMessages"),chatInput:document.getElementById("chatInput"),sendChatBtn:document.getElementById("sendChatBtn"),recordVoiceBtn:null,voiceStatus:null};
const ctx=els.board.getContext("2d");
async function api(path,options={}){const headers={"Content-Type":"application/json","X-Session-Id":state.sessionId,...(options.headers||{})};const response=await fetch(path,{...options,headers});const sessionId=response.headers.get("X-Session-Id");if(sessionId){state.sessionId=sessionId;localStorage.setItem("wuziqi_session",sessionId);}const raw=await response.text();let data={};if(raw){try{data=JSON.parse(raw);}catch(_e){throw new Error(raw.trim()||"请求失败。");}}if(!response.ok||data.ok===false)throw new Error(data.error||"请求失败。");return data;}
function showScreen(screen){state.screen=screen;els.landingScreen.classList.toggle("hidden",screen!=="landing");els.onlineSetupScreen.classList.toggle("hidden",screen!=="online-setup");els.gameScreen.classList.toggle("hidden",screen!=="game");}
function syncPanels(){const localGame=state.screen==="game"&&state.mode==="local";const onlineGame=state.screen==="game"&&state.mode==="room";els.localActions.classList.toggle("hidden",!localGame);els.onlineActions.classList.toggle("hidden",!onlineGame);els.chatCard.classList.toggle("hidden",!onlineGame);}
function setStatus(text){els.status.textContent=text;} function setSetupStatus(text){els.setupStatus.textContent=text;}
function roomSocketOpen(){return typeof WebSocket!=="undefined"&&!!(state.roomSocket&&state.roomSocket.readyState===WebSocket.OPEN);}
function disconnectRoomSocket({suppressReconnect=true}={}){state.suppressSocketReconnect=suppressReconnect;if(state.roomSocketReconnect){clearTimeout(state.roomSocketReconnect);state.roomSocketReconnect=null;}if(!state.roomSocket)return;const socket=state.roomSocket;state.roomSocket=null;try{socket.close();}catch(_e){}}
function scheduleRoomSocketReconnect(){if(state.suppressSocketReconnect||!state.roomCode||state.mode!=="room"||state.screen!=="game"||state.roomSocketReconnect)return;state.roomSocketReconnect=setTimeout(()=>{state.roomSocketReconnect=null;connectRoomSocket();},1500);}
function ensureGameScreenForEnteredRoom(){if(state.mode==="room"&&state.room&&state.room.match_entered&&state.screen==="online-setup"){showScreen("game");setStatus("房主已让双方进入对局。");connectRoomSocket();}}
function handleRoomSocketMessage(payload){if(payload.type==="room_state"&&payload.room){state.room=payload.room;ensureGameScreenForEnteredRoom();updateInfo();return;}if(payload.type==="room_closed"){disconnectRoomSocket();handleApiError(new Error(payload.error||"房间不存在或已过期。"));}}
function connectRoomSocket(){if(typeof WebSocket==="undefined"||!state.roomCode||!state.sessionId||state.mode!=="room"||state.screen!=="game"||roomSocketOpen())return;disconnectRoomSocket({suppressReconnect:false});state.suppressSocketReconnect=false;const protocol=location.protocol==="https:"?"wss":"ws";const url=`${protocol}://${location.host}/ws?code=${encodeURIComponent(state.roomCode)}&session=${encodeURIComponent(state.sessionId)}`;const socket=new WebSocket(url);state.roomSocket=socket;socket.addEventListener("open",()=>{if(state.roomSocket===socket)updateInfo();});socket.addEventListener("message",(event)=>{try{handleRoomSocketMessage(JSON.parse(event.data));}catch(_e){setStatus("收到了一条无效的实时消息。");}});socket.addEventListener("close",()=>{if(state.roomSocket===socket)state.roomSocket=null;if(!state.suppressSocketReconnect){scheduleRoomSocketReconnect();if(state.roomCode)setStatus("实时连接已断开，正在尝试重连。");}});}
function toCanvas(x,y){return [MARGIN+x*CELL,MARGIN+y*CELL];} function fromCanvas(px,py){const x=Math.round((px-MARGIN)/CELL),y=Math.round((py-MARGIN)/CELL);return x<0||x>=SIZE||y<0||y>=SIZE?null:[x,y];}
function formatMove(move){return move?`${move[0]+1},${move[1]+1}`:"无";} function formatHintMove(move){return move?`推荐落点：${move[0]+1},${move[1]+1}`:"推荐落点：无";}
function drawBoard(payload,highlight,hintMove=null){ctx.clearRect(0,0,els.board.width,els.board.height);ctx.fillStyle="#d4ad72";ctx.fillRect(0,0,els.board.width,els.board.height);ctx.strokeStyle="#6b4a25";ctx.lineWidth=1.4;for(let i=0;i<SIZE;i+=1){const offset=MARGIN+i*CELL;ctx.beginPath();ctx.moveTo(MARGIN,offset);ctx.lineTo(MARGIN+CELL*(SIZE-1),offset);ctx.stroke();ctx.beginPath();ctx.moveTo(offset,MARGIN);ctx.lineTo(offset,MARGIN+CELL*(SIZE-1));ctx.stroke();}[3,7,11].forEach((x)=>[3,7,11].forEach((y)=>{const [cx,cy]=toCanvas(x,y);ctx.fillStyle="#3b2f1a";ctx.beginPath();ctx.arc(cx,cy,4,0,Math.PI*2);ctx.fill();}));for(let x=0;x<SIZE;x+=1){for(let y=0;y<SIZE;y+=1){const stone=payload[x][y];if(!stone)continue;const [cx,cy]=toCanvas(x,y);ctx.beginPath();ctx.arc(cx,cy,RADIUS,0,Math.PI*2);if(stone===1){ctx.fillStyle="#111";ctx.fill();ctx.strokeStyle="#525252";}else{ctx.fillStyle="#fbfaf7";ctx.fill();ctx.strokeStyle="#7c7c7c";}ctx.stroke();}}if(highlight){const [cx,cy]=toCanvas(highlight[0],highlight[1]);ctx.strokeStyle="#d9480f";ctx.lineWidth=2;ctx.strokeRect(cx-18,cy-18,36,36);ctx.fillStyle="#d9480f";ctx.beginPath();ctx.arc(cx,cy,3,0,Math.PI*2);ctx.fill();}if(hintMove){const [cx,cy]=toCanvas(hintMove[0],hintMove[1]);ctx.save();ctx.strokeStyle="#1769aa";ctx.lineWidth=2;ctx.setLineDash([6,4]);ctx.strokeRect(cx-20,cy-20,40,40);ctx.setLineDash([]);ctx.fillStyle="#1769aa";ctx.beginPath();ctx.arc(cx,cy,4,0,Math.PI*2);ctx.fill();ctx.restore();}if(state.hoverPoint){const[hx,hy]=state.hoverPoint;const[cx,cy]=toCanvas(hx,hy);ctx.save();ctx.strokeStyle="rgba(217,72,15,0.7)";ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(cx-16,cy);ctx.lineTo(cx+16,cy);ctx.stroke();ctx.beginPath();ctx.moveTo(cx,cy-16);ctx.lineTo(cx,cy+16);ctx.stroke();const canPreview=(state.mode==="local"&&state.local&&state.local.winner==="empty"&&state.local.current_turn==="black"&&payload[hx][hy]===0)||(state.mode==="room"&&state.room&&state.room.winner==="empty"&&state.room.your_turn&&payload[hx][hy]===0&&state.room.match_entered);if(canPreview){ctx.globalAlpha=0.45;ctx.fillStyle=state.mode==="local"?"#111":(state.room&&state.room.your_stone==="white"?"#fbfaf7":"#111");ctx.strokeStyle="#666";ctx.beginPath();ctx.arc(cx,cy,RADIUS,0,Math.PI*2);ctx.fill();ctx.stroke();ctx.globalAlpha=1;}ctx.restore();}}
function renderHintPanel(hint){if(!hint||!hint.move){els.hintPanel.classList.add("hidden");els.hintMoveLabel.textContent="推荐落点：无";els.hintReasonLabel.textContent="你可以使用本局唯一一次 AI 提示来获取推荐落点和理由。";return;}els.hintPanel.classList.remove("hidden");els.hintMoveLabel.textContent=formatHintMove(hint.move);els.hintReasonLabel.textContent=hint.reason||"AI 认为这是当前局面下更稳妥的一手。";}
function renderSetupRoomCard(){const joined=state.mode==="room"&&!!state.roomCode&&!!state.room;const room=state.room;const isHost=!!(room&&room.is_host);const canEnterMatch=!!(joined&&isHost&&(room.connected_count||0)>=2&&!room.match_entered);els.enterCreatedRoomBtn.disabled=!canEnterMatch;els.leaveSetupRoomBtn.disabled=!joined;els.copyCreatedRoomBtn.classList.toggle("hidden",!joined);els.roomCodePill.classList.toggle("hidden",!joined);if(!joined){els.roomStatusText.textContent="当前还没有加入房间。";els.roomCodePill.textContent="房间码 ------";els.roomPlayerList.innerHTML='<div class="muted-text">创建房间后会在这里显示房间码和玩家席位。</div>';return;}const blackName=room.players?.black||"等待加入";const whiteName=room.players?.white||"等待加入";const yourStone=room.your_stone||"empty";let statusText=`房间已就绪，当前 ${room.connected_count}/2 人。`;if(room.match_entered){statusText+=" 双方已经进入对局。";}else if(isHost){statusText+=canEnterMatch?" 你可以点击“进入对局”。":" 等待另一位玩家加入后再进入对局。";}else{statusText+=" 等待房主进入对局。";}if(room.winner!=="empty")statusText+=" 本局已结束。";els.roomStatusText.textContent=statusText;els.roomCodePill.textContent=`房间码 ${state.roomCode}`;els.roomPlayerList.innerHTML=`<div class="room-player-item ${yourStone==="black"?"current-room-player":""}"><div><strong>黑棋</strong><span>${blackName}${yourStone==="black"?"（你）":""}</span></div><div class="muted-text">${room.players?.black?(isHost&&yourStone==="black"?"房主":"已入座"):"等待加入"}</div></div><div class="room-player-item ${yourStone==="white"?"current-room-player":""}"><div><strong>白棋</strong><span>${whiteName}${yourStone==="white"?"（你）":""}</span></div><div class="muted-text">${room.players?.white?"已入座":"等待加入"}</div></div>`;}
function ensureVoiceControls(){if(els.recordVoiceBtn&&els.voiceStatus)return;const toolbar=els.chatCard.querySelector(".chat-toolbar");if(!toolbar)return;const row=document.createElement("div");row.className="inline voice-row";const button=document.createElement("button");button.type="button";button.id="recordVoiceBtn";button.className="accent";button.textContent="录制语音";const status=document.createElement("div");status.id="voiceStatus";status.className="voice-status";status.textContent="单条语音最长 15 秒。";row.appendChild(button);row.appendChild(status);toolbar.appendChild(row);els.recordVoiceBtn=button;els.voiceStatus=status;button.addEventListener("click",()=>toggleVoiceRecording().catch(handleApiError));}
function setVoiceStatus(text){ensureVoiceControls();els.voiceStatus.textContent=text;}
function renderChat(messages){ensureVoiceControls();els.chatMessages.innerHTML="";if(!messages||messages.length===0){const empty=document.createElement("div");empty.className="chat-empty";empty.textContent="当前还没有聊天消息。";els.chatMessages.appendChild(empty);return;}messages.forEach((msg)=>{const item=document.createElement("div");item.className=`chat-item${msg.system?" system":""}${msg.from_you?" self":""}`;const meta=document.createElement("div");meta.className="chat-meta";const sender=document.createElement("div");sender.className="chat-sender";sender.textContent=msg.system?"系统":(msg.from_you?`${msg.sender}（你）`:msg.sender);const time=document.createElement("div");time.className="chat-time";time.textContent=new Date((msg.timestamp||0)*1000).toLocaleTimeString([],{hour:"2-digit",minute:"2-digit"});const body=document.createElement("div");body.className="chat-text";if(msg.message_type==="voice"&&msg.audio_data){const label=document.createElement("div");label.textContent=msg.text||"语音消息";const audio=document.createElement("audio");audio.controls=true;audio.preload="metadata";audio.src=msg.audio_data;body.appendChild(label);body.appendChild(audio);}else{body.textContent=msg.text;}meta.appendChild(sender);meta.appendChild(time);item.appendChild(meta);item.appendChild(body);els.chatMessages.appendChild(item);});els.chatMessages.scrollTop=els.chatMessages.scrollHeight;}
function blobToDataUrl(blob){return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onloadend=()=>resolve(String(reader.result||""));reader.onerror=()=>reject(new Error("无法读取录制完成的语音。"));reader.readAsDataURL(blob);});}
function stopVoiceUi(){if(state.voiceTimer){clearInterval(state.voiceTimer);state.voiceTimer=null;}if(state.mediaRecorder&&state.mediaRecorder.state==="recording"){state.mediaRecorder.stop();return;}if(els.recordVoiceBtn)els.recordVoiceBtn.textContent="录制语音";}
async function startVoiceRecording(){if(!state.roomCode)throw new Error("请先创建或加入房间。");if(!navigator.mediaDevices||!navigator.mediaDevices.getUserMedia)throw new Error("当前浏览器不支持麦克风录音。");let stream;try{stream=await navigator.mediaDevices.getUserMedia({audio:true});}catch(error){if(error&&error.name==="NotAllowedError")throw new Error("系统拒绝了麦克风权限。");if(error&&error.name==="NotFoundError")throw new Error("没有检测到可用麦克风。");if(error&&error.name==="NotReadableError")throw new Error("麦克风不可用，或被其他程序占用。");throw new Error("麦克风录音失败。");}const mimeType=typeof MediaRecorder!=="undefined"&&MediaRecorder.isTypeSupported("audio/webm;codecs=opus")?"audio/webm;codecs=opus":"";const recorder=mimeType?new MediaRecorder(stream,{mimeType}):new MediaRecorder(stream);state.mediaRecorder=recorder;state.voiceChunks=[];state.voiceStartAt=Date.now();recorder.addEventListener("dataavailable",(event)=>{if(event.data&&event.data.size>0)state.voiceChunks.push(event.data);});recorder.addEventListener("stop",async()=>{const durationSeconds=Math.max(1,Math.min(15,Math.round((Date.now()-state.voiceStartAt)/1000)));const chunks=state.voiceChunks.slice();state.mediaRecorder=null;state.voiceChunks=[];stopVoiceUi();stream.getTracks().forEach((track)=>track.stop());if(chunks.length===0){setVoiceStatus("录音已取消。");return;}try{setVoiceStatus("正在上传语音...");const blob=new Blob(chunks,{type:recorder.mimeType||"audio/webm"});const audioData=await blobToDataUrl(blob);const data=await api("/api/rooms/chat",{method:"POST",body:JSON.stringify({code:state.roomCode,type:"voice",audio_data:audioData,mime_type:recorder.mimeType||"audio/webm",duration_seconds:durationSeconds})});state.room=data.room;setStatus("语音消息已发送。");setVoiceStatus("语音发送成功。");updateInfo();}catch(error){setVoiceStatus("语音上传失败。");handleApiError(error);}});recorder.start();if(els.recordVoiceBtn)els.recordVoiceBtn.textContent="停止录音";setVoiceStatus("录音中... 剩余 15 秒");state.voiceTimer=setInterval(()=>{const elapsed=Math.floor((Date.now()-state.voiceStartAt)/1000);const left=Math.max(0,15-elapsed);setVoiceStatus(`录音中... 剩余 ${left} 秒`);if(left<=0&&state.mediaRecorder&&state.mediaRecorder.state==="recording")state.mediaRecorder.stop();},250);}
async function toggleVoiceRecording(){ensureVoiceControls();if(typeof MediaRecorder==="undefined")throw new Error("当前浏览器不支持语音录制。");if(state.mediaRecorder&&state.mediaRecorder.state==="recording"){state.mediaRecorder.stop();return;}await startVoiceRecording();}
function updateInfo(){syncPanels();renderSetupRoomCard();if(state.mode==="local"&&state.local){els.modeLabel.textContent="本地人机";if(state.local.winner!=="empty"){els.turnLabel.textContent="对局结束";setStatus(state.local.winner==="black"?"玩家获胜。":"AI 获胜。");}else{els.turnLabel.textContent=state.local.current_turn==="black"?"你回合":"AI 回合";}els.turnTimerLabel.textContent="无";els.hintUsageLabel.textContent="无";els.lastMoveLabel.textContent=formatMove(state.local.last_opponent_move);els.linkStateLabel.textContent="本地";els.hintRoomBtn.disabled=true;els.hintRoomBtn.textContent="使用 AI 提示";els.acceptUndoBtn.classList.add("hidden");els.rejectUndoBtn.classList.add("hidden");renderHintPanel(null);drawBoard(state.local.board,state.local.current_turn==="black"?state.local.last_opponent_move:null,null);renderChat([]);return;}if(state.mode==="room"&&state.room){els.modeLabel.textContent="联机对战";if(!state.room.match_entered){els.turnLabel.textContent="等待开始";if(state.screen==="game")setStatus(state.room.is_host?"等待你点击“进入对局”。":"等待房主让双方进入对局。");}else if(state.room.winner!=="empty"){els.turnLabel.textContent="对局结束";if(state.room.win_reason==="timeout"){setStatus(state.room.winner===state.room.your_stone?"你超时获胜。":"你超时落败。");}else if(state.room.winner===state.room.your_stone){setStatus("你赢了这局。");}else{setStatus("对手赢了这局。");}}else{els.turnLabel.textContent=state.room.your_turn?"你回合":"对手回合";}if(!state.room.match_entered){els.turnTimerLabel.textContent="未开始";}else if(state.room.timer_pause_reason==="ai-hint"){els.turnTimerLabel.textContent=`AI 提示暂停中（${state.room.hint_pause_remaining_seconds} 秒）`;}else if(state.room.turn_timer_active){els.turnTimerLabel.textContent=`${state.room.turn_time_left_seconds} 秒 / ${state.room.turn_time_limit_seconds} 秒`;}else{els.turnTimerLabel.textContent="暂停";}els.hintUsageLabel.textContent=state.room.hint_used?"已使用":"未使用";const canUseHint=state.room.match_entered&&!state.room.hint_used&&state.room.your_turn&&state.room.winner==="empty";els.hintRoomBtn.disabled=!canUseHint;els.hintRoomBtn.textContent=state.room.hint_used?"AI 提示已用":"使用 AI 提示";renderHintPanel(state.room.active_hint||null);els.lastMoveLabel.textContent=formatMove(state.room.opponent_last_move);if(state.room.pending_undo_request){els.linkStateLabel.textContent=state.room.pending_undo_from_you?"已发送悔棋请求":"收到悔棋请求";}else if(!state.room.match_entered){els.linkStateLabel.textContent=state.room.is_host?"等待你开始对局":"等待房主开始对局";}else{els.linkStateLabel.textContent=`${state.room.connected_count}/2 已连接${roomSocketOpen()?" · 实时同步中":" · 正在重连"}`;}const showDecision=state.room.pending_undo_request&&!state.room.pending_undo_from_you;els.acceptUndoBtn.classList.toggle("hidden",!showDecision);els.rejectUndoBtn.classList.toggle("hidden",!showDecision);drawBoard(state.room.board,state.room.your_turn?state.room.opponent_last_move:null,state.room.active_hint?state.room.active_hint.move:null);renderChat(state.room.chat_messages||[]);return;}els.modeLabel.textContent="本地人机";els.turnLabel.textContent="等待开始";els.turnTimerLabel.textContent="无";els.hintUsageLabel.textContent="无";els.lastMoveLabel.textContent="无";els.linkStateLabel.textContent="未连接";els.acceptUndoBtn.classList.add("hidden");els.rejectUndoBtn.classList.add("hidden");els.hintRoomBtn.disabled=true;els.hintRoomBtn.textContent="使用 AI 提示";renderHintPanel(null);renderChat([]);}
function handleApiError(error){const message=error.message||"请求失败。";if(message.includes("房间不存在或已过期")){disconnectRoomSocket();state.room=null;state.roomCode="";els.roomCodeInput.value="";els.acceptUndoBtn.classList.add("hidden");els.rejectUndoBtn.classList.add("hidden");if(state.screen==="game"&&state.mode==="room")showScreen("online-setup");}if(state.screen==="online-setup")setSetupStatus(message);else setStatus(message);updateInfo();}
async function leaveRoom({silent=false}={}){stopVoiceUi();disconnectRoomSocket();if(!state.roomCode||state.leavingRoom)return;state.leavingRoom=true;const code=state.roomCode;try{await api("/api/rooms/leave",{method:"POST",body:JSON.stringify({code})});}catch(error){if(!silent)handleApiError(error);}finally{state.room=null;state.roomCode="";els.roomCodeInput.value="";els.acceptUndoBtn.classList.add("hidden");els.rejectUndoBtn.classList.add("hidden");state.leavingRoom=false;}}
function leaveRoomOnUnload(){stopVoiceUi();disconnectRoomSocket();if(!state.roomCode)return;const payload=JSON.stringify({code:state.roomCode});const blob=new Blob([payload],{type:"application/json"});navigator.sendBeacon("/api/rooms/leave",blob);}
async function goToLanding(){stopVoiceUi();disconnectRoomSocket();if(state.roomCode)await leaveRoom({silent:true});state.mode="local";state.local=null;state.room=null;state.roomCode="";showScreen("landing");setSetupStatus("创建房间或输入房间码加入。");setStatus("准备开始");updateInfo();}
async function enterLocalMode(){showScreen("game");state.mode="local";await startLocal();}
function enterOnlineSetup(){disconnectRoomSocket({suppressReconnect:true});state.mode="room";showScreen("online-setup");setSetupStatus("创建房间或输入房间码加入。");updateInfo();}
async function startLocal(){disconnectRoomSocket();if(state.roomCode)await leaveRoom({silent:true});const data=await api("/api/local/new",{method:"POST",body:JSON.stringify({depth:Number(els.depthSelect.value)})});state.mode="local";state.local=data.state;state.room=null;state.roomCode="";showScreen("game");setStatus("新的本地对局已开始。");updateInfo();}
async function localUndo(){const data=await api("/api/local/undo",{method:"POST",body:"{}"});state.local=data.state;setStatus(data.ok?"已完成悔棋。":"当前没有可悔的棋。");updateInfo();}
async function createRoom(){if(state.roomCode)await leaveRoom({silent:true});const name=els.playerNameInput.value.trim()||"房主";const turnLimitSeconds=Number(els.turnLimitSelect.value||30);const data=await api("/api/rooms/create",{method:"POST",body:JSON.stringify({name,turn_limit_seconds:turnLimitSeconds})});state.mode="room";state.room=data.room;state.roomCode=data.room.code;els.roomCodeInput.value=data.room.code;showScreen("online-setup");setSetupStatus(`房间已创建：${data.room.code}，请复制房间码并等待另一位玩家加入。`);updateInfo();}
async function joinRoom(){if(state.roomCode)await leaveRoom({silent:true});const code=els.roomCodeInput.value.trim().toUpperCase();const name=els.playerNameInput.value.trim()||"玩家";const data=await api("/api/rooms/join",{method:"POST",body:JSON.stringify({code,name})});state.mode="room";state.room=data.room;state.roomCode=data.room.code;els.roomCodeInput.value=data.room.code;showScreen("online-setup");setSetupStatus("已加入房间，等待房主让双方进入对局。");updateInfo();}
async function enterCreatedRoom(){if(!state.roomCode)throw new Error("当前没有可进入的房间。");const data=await api("/api/rooms/enter",{method:"POST",body:JSON.stringify({code:state.roomCode})});state.room=data.room;showScreen("game");setStatus("房主已让双方进入对局。");updateInfo();connectRoomSocket();}
async function leaveSetupRoom(){await leaveRoom({silent:false});showScreen("online-setup");setSetupStatus("你已离开房间。你可以重新创建房间或输入房间码加入。");updateInfo();}
async function roomUndo(action="request"){const data=await api("/api/rooms/undo",{method:"POST",body:JSON.stringify({code:state.roomCode,action})});state.room=data.room;setStatus({request:"悔棋请求已发送。",accept:"已同意悔棋。",reject:"已拒绝悔棋。"}[action]);updateInfo();}
async function resetRoom(){const data=await api("/api/rooms/reset",{method:"POST",body:JSON.stringify({code:state.roomCode})});state.room=data.room;showScreen("online-setup");setSetupStatus("棋局已重开，请等待房主再次让双方进入对局。");updateInfo();}
async function requestRoomHint(){if(!state.roomCode){setStatus("请先创建或加入房间。");return;}const data=await api("/api/rooms/hint",{method:"POST",body:JSON.stringify({code:state.roomCode})});state.room=data.room;const moveText=state.room.active_hint&&state.room.active_hint.move?formatMove(state.room.active_hint.move):"无";setStatus(`AI 提示已生成：${moveText}`);updateInfo();}
async function sendChat(){if(!state.roomCode){setStatus("请先创建或加入房间。");return;}const text=els.chatInput.value.trim();if(!text){setStatus("消息内容不能为空。");return;}const data=await api("/api/rooms/chat",{method:"POST",body:JSON.stringify({code:state.roomCode,text})});state.room=data.room;els.chatInput.value="";setStatus("消息已发送。");updateInfo();}
async function refresh(){try{if(state.mode==="local"&&state.screen==="game"){if(state.local&&state.local.winner!=="empty"){updateInfo();return;}const data=await api("/api/local/state");state.local=data.state;updateInfo();return;}if(state.mode==="room"&&state.roomCode){if(state.screen==="game"&&!roomSocketOpen()&&!state.roomSocketReconnect)connectRoomSocket();if(state.screen==="online-setup"||!roomSocketOpen()){const data=await api(`/api/rooms/state?code=${encodeURIComponent(state.roomCode)}`);state.room=data.room;ensureGameScreenForEnteredRoom();updateInfo();}}}catch(error){handleApiError(error);}}
els.board.addEventListener("click",async(event)=>{if(state.screen!=="game")return;const rect=els.board.getBoundingClientRect();const scaleX=els.board.width/rect.width,scaleY=els.board.height/rect.height;const point=fromCanvas((event.clientX-rect.left)*scaleX,(event.clientY-rect.top)*scaleY);if(!point)return;try{if(state.mode==="local"){if(state.local&&state.local.winner!=="empty"){setStatus("本局已结束，请重新开始或悔棋后继续。");return;}const data=await api("/api/local/move",{method:"POST",body:JSON.stringify({x:point[0],y:point[1]})});state.local=data.state;if(state.local.winner==="empty")setStatus("已落子。");updateInfo();return;}if(state.mode==="room"&&state.roomCode){if(state.room&&!state.room.match_entered){setStatus("房主还没有让双方进入对局。");return;}if(state.room&&state.room.winner!=="empty"){setStatus("联机对局已结束，请重开棋局后继续。");return;}const data=await api("/api/rooms/move",{method:"POST",body:JSON.stringify({code:state.roomCode,x:point[0],y:point[1]})});state.room=data.room;if(state.room.winner==="empty")setStatus("已落子。");updateInfo();}}catch(error){handleApiError(error);}});
els.board.addEventListener("mousemove",(event)=>{if(state.screen!=="game")return;const rect=els.board.getBoundingClientRect();const scaleX=els.board.width/rect.width,scaleY=els.board.height/rect.height;state.hoverPoint=fromCanvas((event.clientX-rect.left)*scaleX,(event.clientY-rect.top)*scaleY);if(state.mode==="local"&&state.local){drawBoard(state.local.board,state.local.current_turn==="black"?state.local.last_opponent_move:null,null);return;}if(state.mode==="room"&&state.room){drawBoard(state.room.board,state.room.your_turn?state.room.opponent_last_move:null,state.room.active_hint?state.room.active_hint.move:null);}});
els.board.addEventListener("mouseleave",()=>{state.hoverPoint=null;if(state.mode==="local"&&state.local){drawBoard(state.local.board,state.local.current_turn==="black"?state.local.last_opponent_move:null,null);return;}if(state.mode==="room"&&state.room){drawBoard(state.room.board,state.room.your_turn?state.room.opponent_last_move:null,state.room.active_hint?state.room.active_hint.move:null);}});
async function copyActiveRoomCode(){if(!state.roomCode&&!els.roomCodeInput.value.trim()){if(state.screen==="online-setup")setSetupStatus("当前没有可复制的房间码。");else setStatus("当前没有可复制的房间码。");return;}const code=state.roomCode||els.roomCodeInput.value.trim().toUpperCase();await navigator.clipboard.writeText(code);if(state.screen==="online-setup")setSetupStatus(`房间码已复制：${code}`);else setStatus(`房间码已复制：${code}`);}
els.goLocalBtn.addEventListener("click",()=>enterLocalMode().catch(handleApiError));els.goOnlineBtn.addEventListener("click",enterOnlineSetup);els.backToHomeFromSetupBtn.addEventListener("click",()=>goToLanding().catch(handleApiError));els.backHomeBtn.addEventListener("click",()=>goToLanding().catch(handleApiError));els.startLocalBtn.addEventListener("click",()=>startLocal().catch(handleApiError));els.undoLocalBtn.addEventListener("click",()=>localUndo().catch(handleApiError));els.createRoomBtn.addEventListener("click",()=>createRoom().catch(handleApiError));els.joinRoomBtn.addEventListener("click",()=>joinRoom().catch(handleApiError));els.copyCreatedRoomBtn.addEventListener("click",()=>copyActiveRoomCode().catch(handleApiError));els.enterCreatedRoomBtn.addEventListener("click",()=>enterCreatedRoom().catch(handleApiError));els.leaveSetupRoomBtn.addEventListener("click",()=>leaveSetupRoom().catch(handleApiError));els.hintRoomBtn.addEventListener("click",()=>requestRoomHint().catch(handleApiError));els.undoRoomBtn.addEventListener("click",()=>roomUndo().catch(handleApiError));els.resetRoomBtn.addEventListener("click",()=>resetRoom().catch(handleApiError));els.acceptUndoBtn.addEventListener("click",()=>roomUndo("accept").catch(handleApiError));els.rejectUndoBtn.addEventListener("click",()=>roomUndo("reject").catch(handleApiError));els.sendChatBtn.addEventListener("click",()=>sendChat().catch(handleApiError));els.chatInput.addEventListener("keydown",(event)=>{if(event.key==="Enter"){event.preventDefault();sendChat().catch(handleApiError);}});window.addEventListener("pagehide",leaveRoomOnUnload);
showScreen("landing");syncPanels();ensureVoiceControls();setSetupStatus("创建房间或输入房间码加入。");setStatus("准备开始");drawBoard(Array.from({length:SIZE},()=>Array(SIZE).fill(0)),null);updateInfo();setInterval(refresh,POLL_MS);
